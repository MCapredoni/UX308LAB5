// Calculate the **minutes** of lawn mowing. In **metres** start with a variable that references the **length**, and another for **width**, and another for **squaremetres**.

function convertLawnSize(length, width, metres){
    let metres = length * width;
    return minutes;
}

export {convertLawnSize}